# Copy to .env and fill with your OAuth credentials and optionally an explicit FRONTEND_REDIRECT
# Example values below are placeholders — create OAuth credentials in Google Cloud Console and paste the values.

GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Optional: override the redirect the server expects (defaults to http://localhost:3000/oauth_google_callback)
FRONTEND_REDIRECT=http://localhost:3000/oauth_google_callback

# Optional: GitHub OAuth (frontend & server)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

